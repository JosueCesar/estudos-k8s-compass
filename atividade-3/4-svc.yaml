apiVersion: v1
kind: Service
metadata:
  name: httpd-service-nautilus
  namespace: labapache
spec:
  type: NodePort
  selector:
    app: httpd_app_nautilus
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30004